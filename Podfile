platform :ios, '10.0'

target 'Xcode12Sandbox' do
  use_frameworks!

  pod 'MyPod', :path => 'MyPod/', :testspecs => ['Tests']

  target 'Xcode12SandboxTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
